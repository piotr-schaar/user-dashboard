import styled from 'styled-components';

const Card = styled.div`
  padding: 25px;
  width: 50%;
  color: white;
  position: relative;
  background: rgba(54, 64, 99, 0.7);
  border-radius: 10px;
`;

export default Card;
