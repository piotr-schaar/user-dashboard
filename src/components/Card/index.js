import styled from 'styled-components';

const Card = styled.div`
  color: white;
  width: 100%;
  height: 100%;
  padding: 10px;
  background: rgba(54, 64, 99, 0.4);
  position: relative;
`;

export default Card;
