import styled from 'styled-components';

const Card = styled.div`
  width: 50%;
  color: white;
  padding: 10px;
  background: rgba(54, 64, 99, 0.7);
  position: relative;
  border-radius: 10px;
`;

export default Card;
