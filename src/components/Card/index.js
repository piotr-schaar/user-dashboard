import styled from 'styled-components';

const Card = styled.div`
  color: white;
  padding: 10px;
  background: rgba(54, 64, 99, 0.4);
  position: relative;
  border-radius: 10px;
`;

export default Card;
